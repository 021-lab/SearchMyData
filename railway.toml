[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/tasks/search"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
